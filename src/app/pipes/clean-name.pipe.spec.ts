import { CleanNamePipe } from './clean-name.pipe';

describe('CleanNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CleanNamePipe();
    expect(pipe).toBeTruthy();
  });
});
